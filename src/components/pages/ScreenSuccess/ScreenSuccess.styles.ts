import { styled } from '@mui/material/styles';
import { Container } from '@mui/material';

export const StyledScreenSuccess = styled(Container)(({ theme }) => ({
  width: '100vw',
  height: '70vh',
  display: 'flex',
  alignItems: 'center',
}));
